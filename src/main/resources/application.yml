server:
    port: 12345
    servlet:
        context-path: /api/v1
logging:
    level:
        com:
            example:
                dogservice: DEBUG

spring:
    datasource:
        url: jdbc:mysql://localhost:3306/db1
        username: root
        password: 12345678
        driver-class-name: com.mysql.jdbc.Driver
    jpa:
        hibernate.ddl-auto: validate